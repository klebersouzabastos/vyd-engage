{
  "name": "vyd-engage-extension",
  "version": "1.0.0",
  "private": true,
  "description": "Extensão Chrome (MV3) do VYD Engage — Copiloto no WhatsApp Web (Upgrade RD, req 24).",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "package": "node build.mjs --zip",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "esbuild": "^0.21.5",
    "typescript": "^5.4.0"
  }
}
